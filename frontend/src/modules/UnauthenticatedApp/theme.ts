import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#039BE5", contrastText: "#ffffff" },
    background: { default: "#F1F8FD" }
  }
});

export default theme;
